class A {
  test() {
	  print "A.test() called."; // This is the method that should be called first after chap 29 chal 3.
		inner();
	}
}
class B < A {
  test() {
	  print "B.test() called."; // This is the method that should be called second after chap 29 chal 3.
		inner();
	}
}
class C < B {
}
class D < C {
  test() {
	  print "D.test() called."; // This is the method that is currently called prior to chap 29 chal 3.
	}
}

var d = D();
d.test();
